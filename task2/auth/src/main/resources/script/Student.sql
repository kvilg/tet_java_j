CREATE TABLE Student (
   id INTEGER PRIMARY KEY AUTOINCREMENT,
   first_name VARCHAR(50),
   last_name VARCHAR(50),
   patronymic VARCHAR(50),
   date_of_birth Date,
   groupStudent VARCHAR(50),
);